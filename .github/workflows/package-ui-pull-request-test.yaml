name: UI/PR - build and test

on:
  pull_request:
    branches: [main, staging, development]
    paths:
      - "packages/ui/**"
      - "yarn.lock"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: "${{ secrets.ACCESS_TOKEN }}"

      - uses: actions/setup-node@v3
        with:
          node-version: "16"
          cache: "npm"

      - name: Install `foundry`
        uses: onbjerg/foundry-toolchain@v1
        with:
          version: nightly

      - name: Install `packages`
        run: yarn install
        env:
          YARN_CHECKSUM_BEHAVIOR: update

      - name: Install `foundry libs`
        run: yarn workspace @midas-capital/sdk foundry install

      - name: Lint `ui`
        run: yarn workspace @midas-capital/ui lint

      - name: Build `sdk`
        run: yarn workspace @midas-capital/sdk build

      - name: Build `ui`
        run: yarn workspace @midas-capital/ui build
