{
  "name": "monorepo",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@typescript-eslint/eslint-plugin": "^5.27.0",
    "@typescript-eslint/parser": "^5.27.0",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "@resolver-engine/core": "^0.3.3",
    "ts-node": "^10.8.0"
  },
  "resolutions": {
    "@types/react": "^17.0.15"
  },
  "scripts": {
    "build:typechain": "yarn workspace @midas-capital/sdk typechain",
    "build:sdk": "yarn workspace @midas-capital/sdk build:sdk",
    "build:twap": "yarn workspace @midas-capital/fuse-twap-bot build",
    "build:ui": "yarn workspace @midas-capital/ui build",
    "build:liquidator": "yarn workspace @midas-capital/fuse-liquidator-bot build",
    "build:deploy:liquidator": "yarn build:typechain && yarn build:sdk && yarn build:liquidator",
    "build:deploy:twap": "yarn build:typechain && yarn build:sdk && yarn build:twap",
    "build:deploy:ui": "yarn build:typechain && yarn build:sdk && yarn build:ui",
    "dev:ui": "yarn workspace @midas-capital/sdk export; yarn build:typechain && yarn build:sdk && yarn workspace @midas-capital/ui dev",
    "dev:node": "yarn workspace @midas-capital/sdk hardhat node"
  }
}
